project_name: shctl
builds:
  - id: shctl
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
archives:
  - format: tar.gz
    files:
      - shctl
      - README.md
release:
  github:
    owner: yourusername
    name: shctl
